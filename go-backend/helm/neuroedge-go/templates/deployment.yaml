
{{- include "neuroedge-go.fullname" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "neuroedge-go.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "neuroedge-go.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "neuroedge-go.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.service.port }}
