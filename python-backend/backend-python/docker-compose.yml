version: '3.8'
services:
  neuroedge:
    build: .
    container_name: neuroedge_core
    ports:
      - "8000:8000"
    environment:
      - NEUROEDGE_VEC_DIM=256
      - NEUROEDGE_AUTO_PURGE=0
    volumes:
      - ./backend-python:/app/backend-python
